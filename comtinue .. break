#include <stdio.h>
int main ()
{
	int counter;
	for (counter=0;counter<=10;counter++) {
		if (counter  %2!=0){ 
		continue  ; 
		}
		printf ("%d\n", counter);
	}
	printf ("after the for loop \n");
	return 0;
}
